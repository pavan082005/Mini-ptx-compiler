# Mini-ptx-compiler
Convert Simple Expressions → PTX Assembly
📌 Overview
This project is a mini compiler that translates simple mathematical expressions into PTX (Parallel Thread Execution) assembly code, the intermediate representation used in GPU programming.
The goal of this project is to understand how high-level code is transformed into low-level instructions, similar to how CUDA programs are compiled for NVIDIA GPUs.
